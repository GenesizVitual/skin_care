import 'package:flutter/material.dart';

class NotFountSolution extends StatelessWidget {
  // const NotFountSolution({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("Not Found Solution"),
    );
  }
}
